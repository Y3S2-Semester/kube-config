# Eacademu-kube-config

- This repository houses the kubernetes manifest files which are used by the E-academy system.

### Run on windows
- open git bash and run using `sh run.sh` command

### Run on mac
- `chmod +x mac.sh`
- `./mac.sh`
